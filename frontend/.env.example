# Backend API base URL. Leave as /api when the backend serves the frontend (single-service deploy).
# For split deploys (e.g. Vercel frontend + Render backend), set your backend URL:
# VITE_API_URL=https://your-backend.onrender.com/api
VITE_API_URL=/api
